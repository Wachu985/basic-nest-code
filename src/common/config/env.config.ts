export const EnvConfiguration = () => ({
  // environment: process.env.NODE_ENV || 'dev',
  // mongodb: process.env.MONGODB,
  // default_limit: +process.env.DEFAULT_LIMIT || 40,
  port: +process.env.PORT || 3000,
});
